<template>
    <div>
        <button
            v-if="redirectTo"
            v-link="{path: redirectTo}"
            class="btn btn-default"
        >
            Cancel
        </button>

        <button
            v-if="destroy"
            v-on:click="destroy()"
            v-link="{path: redirectTo}"
            class="btn btn-danger"
        >
            Delete
        </button>

        <button
            v-if="save"
            v-on:click="save()"
            v-link="{path: redirectTo}"
            class="btn btn-success"
        >
            Save
        </button>
    </div>
</template>

<script>
    module.exports = {
        props: [
            'save',
            'destroy',
            'redirectTo'
        ]
    };
</script>